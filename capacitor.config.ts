import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hoopstats.app',
  appName: 'HoopStats',
  webDir: 'dist/hoopstats/browser'
};

export default config;
